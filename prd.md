# 科研选题副驾系统（Research Topic Co-pilot）PRD

### 1. 目标用户与核心任务

- **目标用户**：科研初学者（高年级本科生、研一新生）。
- **核心痛点**：
    - 想法过于模糊，发散过度。
    - 不了解技术前沿，难以判断“值得做什么”。
    - 忽略客观资源限制（算力、数据、预算、时间）。
    - 多个潜在方向难以取舍，无法收敛。
- **核心任务**：通过“意图对齐、情报侦察、路径比较、决策收敛”四步逻辑，帮助用户从一个模糊兴趣点出发，产出一份**具备资源支撑、逻辑闭环、且可落地执行**的开题报告雏形。

---

### 2. 系统边界与能力结构

- **系统边界**：
    - **聚焦选题阶段**：不提供论文润色、代写或实验代码自动生成，仅专注于从“调研”到“决策”的高价值收敛。
    - **硬约束驱动**：将算力、数据、预算、周期等作为硬门槛，不推荐任何不可执行方案。
- **能力结构**：
    - **意图对齐层（Gatekeeper）**：基于有限状态机（FSM）拦截无效输入，完成人机需求对齐。
    - **情报侦察层（Scout Agent）**：利用 Tavily Search 执行 ReAct 范式的实时学术情报抓取。
    - **执行交付层（Convergence）**：将抽象选择转化为结构化的行动项（Action Items）。

---

### 3. 用户输入与系统输出的组织形式

- **用户输入**：
    - **结构化表单**：
        - `research_domain`：研究领域
        - `available_resources`：预算/硬件/数据/时间等
        - `initial_idea`：初始想法
    - **非结构化对话**：`sys.query`（用户自身需求与对本系统的"期望"、用户实时反馈与路径选择命令）。
- **系统输出**：
    - **路由反馈**：状态反馈（追问细节、指出资源冲突等）。
    - **情报矩阵**：领域全景 + 2–3 条差异化候选路径 + 横向对比表。
    - **开题大纲**：暂定题目、核心研究问题（RQ）、数据/技术依赖、未来 48 小时执行清单。

---

### 4. 核心工作流（检索→梳理→比较→收敛）

系统采用**漏斗式架构**，每一层都提升信息确定性：

1. **意图阶段（Diverge & Filter）**：节点 B（LLM）综合分析初始变量与实时对话，判断状态（`INSUFFICIENT / CONFLICT / READY / CONVERGE`）。
2. **数据路由（Data Bus）**：Python 节点解析 LLM 输出 JSON，转为下游强类型变量（`out_status`, `out_keywords`）。
3. **侦察与比较（Scout & Compare）**：Scout Agent 基于关键词联网检索，生成多路径情报矩阵。
4. **决策收敛（Converge & Deliver）**：用户确认选择后，生成避坑指南与开题大纲。

---

### 5. 数据源、引用机制、来源分级与冲突处理

- **数据源**：以 **Tavily Search** 为核心，通过搜索引擎访问 arXiv、ACM Digital Library、IEEE Xplore 及顶级开源社区。
- **引用机制**：强制每一句客观论断附带引用（`[来源名称/URL]`），避免幻觉。
- **来源分级（Source Grading）**：
    - **一级信源（高权重）**：同行评审论文、顶会预印本、官方技术文档。
    - **二级信源（低权重）**：技术博客、开发者讨论。
- **冲突处理（Conflict Resolution）**：若不同信源评价不一，**不做“折中和稀泥”**，而是以“学术争议点（Research Gap）”形式列出，并提示用户可能是创新切入点。

---

### 6. 结果验证与人工确认哨点（Human-in-the-loop）

为保证选题可落地，设置两处人工确认：

1. **关键词确认（HITL-1）**：检索前列出提炼的 `keywords`，询问用户“这些词是否代表你的想法”，防止检索偏离。
2. **路径抉择（HITL-2）**：展示横向对比矩阵后流程强制挂起，只有当用户明确输入“我选路径 X”且被识别为 `CONVERGE`，才进入最终交付。

---

### 7. 评估方案（是否真正帮助科研新手）

- **指标 1：意图收敛轮数**。从模糊表述到进入 `READY` 的平均对话轮数，越少说明引导力越强。
- **指标 2：资源匹配吻合度**。最终建议路径与 `available_resources` 的一致性，避免“很好但做不了”。
- **指标 3：主观决策信心指数**。对比用户在看到矩阵前后的决策焦虑变化。

---

### 8. 核心技术实现（Demo & 伪代码）

#### A. 意图解析层（Python 节点伪代码）

```python
def main(llm_output):
	import json, re
	# 鲁棒性设计：通过正则清洗大模型可能输出的 Markdown 标签
	cleaned_text = re.sub(r'^```[a-zA-Z]*\n', '', llm_output.strip(), flags=re.MULTILINE)
	cleaned_text = re.sub(r'\n```$', '', cleaned_text).strip()
	
	try:
		data = json.loads(cleaned_text)
		# 解析三个关键字段，解耦后续路由逻辑
		return {
			"out_status": data.get("intent_status"),  # READY/CONVERGE 等
			"out_reply": data.get("reply_to_user"),
			"out_keywords": ", ".join(data.get("keywords", []))
		}
	except:
		return {"out_status": "ERROR", "out_reply": "解析失败", "out_keywords": ""}
```

#### B. Scout Agent 逻辑策略（Prompts Logic）

- **核心逻辑**：采用 ReAct 框架。
- 当检测到关键词时，优先检索 `site:arxiv.org`。
- 若 3 轮迭代仍未找到完全匹配结果，触发“最佳推断”机制：基于内部知识库对碎片信息整合输出，保证外部工具故障时流程不被阻断。

---

### 9. 示例输出

以下是系统针对某位用户完成完整流程后的最终交付示例，展示从模糊想法（输入：计算机科学、10w预算、多智能体选股）到可执行开题大纲的完整转化过程：

---

<aside>

## ✅ 最终选题确认：基于元学习与纳什均衡搜索的多智能体投资组合动态博弈策略研究

### 1. 核心研究逻辑 (Research Logic)

- **核心问题 (Research Question)**：在高度非平稳且参与者策略相互影响的金融市场中,如何设计一个多智能体强化学习框架,使其能够快速收敛至(近似)纳什均衡,并动态适应市场体制的突变,从而构建出具有强鲁棒性的投资组合策略?
- **预期创新点**：本研究旨在将**元学习(Meta-Learning)**与**多智能体纳什均衡求解**进行深度耦合。传统MARL在金融场景下容易陷入策略循环或过拟合单一市场状态。本路径的创新在于：
    1. 将市场体制切换(如牛市、熊市、震荡市)建模为不同的"任务",利用元学习器使智能体群学会"如何快速学习",从而在新市场环境下快速找到新均衡;
    2. 设计一种高效的、适用于高维连续动作空间的近似纳什均衡搜索算法,作为智能体策略更新的内在目标,提升整体策略的稳定性和博弈有效性。

### 2. 资源适配与防坑指南 (Feasibility & Risks)

- **匹配评估**：针对你拥有的10万预算,我认为最核心的支撑点是**云计算资源的弹性与定向投入**。预算可以重点配置为：
    - **每年约6-7万元用于租赁高性能GPU云服务器(如AWS p3.2xlarge或同等级别,用于大规模并行模拟与元训练)**
    - **约2万元用于购买覆盖多市场周期、包含极端行情的高质量金融数据(如Datastream或国内Wind的特定板块数据)**
    - 剩余1万元作为备用金,用于论文润色、学术会议注册等。
- **风险提示**：**最大的工程硬伤在于"模拟环境的真实性与计算效率的权衡"**。为了训练元学习器和搜索纳什均衡,需要构建一个能够生成丰富市场体制的模拟器。过于简单的模拟器(如几何布朗运动)会导致学到的策略无法泛化到真实市场;而过于复杂的模拟器(如基于订单簿的Agent-Based Model)计算成本极高,可能超出预算和时限。
- **规避建议**：
    - **Plan B (分层模拟策略)**：先构建一个**中等复杂度的模拟器**作为主训练环境。例如,使用基于历史统计特性(波动率、相关性突变)的机制切换模型,或引入少数具有简单策略的噪声交易者智能体。同时,准备一个**小型但高度真实的子环境**(例如,基于某段时间真实Tick数据的模拟),仅用于最终策略的验证和微调。这样在预算内最大化训练效率,并保留对真实性的检验。
    - **算力监控与动态调整**：在项目开始的前两周,运行一个基准实验,评估单次训练循环的耗时和成本。如果远超预期,立即启动Plan B,或考虑采用**课程学习**先在小规模、低维环境中训练元学习器,再迁移到更复杂环境,而非一开始就进行全尺度训练。

### 3. 落地行动清单 (Next 48 Hours)

- [ ]  **文献深挖**：推荐2个最值得去搜索的特定关键词或必读经典综述。
    1. **关键词组合搜索**：在arXiv或Google Scholar搜索`"meta-learning multi-agent reinforcement learning finance"`以及`"Nash equilibrium approximation continuous action deep reinforcement learning"`。
    2. **必读经典**：精读论文《Model-Agnostic Meta-Learning for Fast Adaptation of Deep Networks》(MAML,元学习奠基作之一)和《Multi-Agent Actor-Critic for Mixed Cooperative-Competitive Environments》(MADDPG,多智能体基准算法),思考如何将MAML的"快速适应"思想与MADDPG的"集中式训练、分布式执行"架构结合,并融入均衡约束。
- [ ]  **实验/数据破冰**：建议第一步要下载的数据集或要跑通的GitHub Demo。
    1. **数据**：立即注册并试用**`yfinance`(Python库)或AKShare**,下载沪深300指数(`000300.SH`)和纳斯达克100指数(`NDX`)近20年的日频数据(价格、成交量)。目标是快速构建一个简单的双资产、双市场体制(以2008年金融危机为界)的回测环境原型。
    2. **代码**：在GitHub上找到并尝试跑通**`ray-project/ray`中的`RLlib`库**提供的`MADDPG`或`PPO`在多智能体粒子环境(`SimpleSpread`)中的示例。这是你构建自己博弈环境的技术起点。
- [ ]  **框架搭建**：建议先完成哪部分的思路梳理。
    
    用一页纸画出你设想的**整体框架图**。明确标出：a) 环境模拟器(包含体制切换逻辑)的输入输出; b) 多个智能体(Actor-Critic)的网络结构; c) 元学习器(Meta-Learner)的位置和它要优化的参数(是智能体的初始参数?还是环境体制的编码?); d) 纳什均衡搜索模块(例如一个正则化项或一个额外的损失函数)如何嵌入到智能体的更新过程中。**先不求细节完美,但求逻辑闭环**。
    

> *"科研的第一步往往是最难的,但既然方向已定,剩下的就是行动。这个选题站在理论与工程的前沿,挑战巨大,但一旦突破,价值也非凡。请务必从行动清单的第一项开始,步步为营。加油,期待看到你的初稿!"*
> 
</aside>
